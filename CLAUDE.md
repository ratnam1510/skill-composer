

<!-- AUTO-GENERATED BY SKILL-COMPOSER — do not edit this section manually -->
<!-- Regenerate with: skill-composer chain -->

## Skill Chains

When a prompt matches one of these chains, invoke the skills in order.
Each skill runs fully before the next begins. The output of one skill becomes context for the next.
Do not load all skills at once — chain them: run the first, then the second, and so on.

### Defined Chains

**Design Pipeline**
> `/frontend-design` → `/animate` → `/polish` → `/audit`
> _Triggers: frontend-design, create, animate, review, polish_

**Design & Ship**
> `/frontend-design` → `/animate` → `/optimize` → `/harden` → `/polish`
> _Triggers: frontend-design, create, animate, review, optimize_

**Design Review**
> `/frontend-design` → `/critique` → `/polish`
> _Triggers: frontend-design, create, critique, evaluate, polish_

**Accessibility**
> `/audit` → `/clarify` → `/harden` → `/adapt`
> _Triggers: audit, perform, clarify, improve, harden_

**Visual Enhancement**
> `/colorize` → `/bolder` → `/animate` → `/delight`
> _Triggers: colorize, add, bolder, amplify, animate_

**Visual Refinement**
> `/quieter` → `/distill` → `/normalize` → `/polish`
> _Triggers: quieter, tone, distill, strip, normalize_

**Component Extraction**
> `/extract` → `/normalize` → `/adapt`
> _Triggers: extract, extract, normalize, normalize, adapt_

**Onboarding Flow**
> `/onboard` → `/clarify` → `/delight` → `/polish`
> _Triggers: onboard, design, clarify, improve, delight_

**Remotion Video**
> `/remotion-best-practices` → `/animations` → `/transitions` → `/audio` → `/timing`
> _Triggers: remotion-best-practices, best, animations, fundamental, transitions_

**Remotion Captions**
> `/transcribe-captions` → `/display-captions` → `/subtitles` → `/timing`
> _Triggers: transcribe-captions, transcribing, display-captions, displaying, subtitles_

**Remotion Media**
> `/videos` → `/images` → `/audio` → `/ffmpeg` → `/trimming`
> _Triggers: videos, embedding, images, embedding, audio_

**Plugin Development**
> `/plugin-structure` → `/skill-development` → `/command-development` → `/agent-development`
> _Triggers: plugin-structure, skill, skill-development, skill, command-development_

_226 skills indexed, 12 chains available._
<!-- END SKILL-COMPOSER -->
